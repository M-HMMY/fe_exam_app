import type { TextbookSection } from '../../types';

export const tDb: TextbookSection[] = [
  {
    id: 't-db-1',
    categoryId: 't-db',
    title: 'データベース設計と正規化',
    goal: 'E-R 図と関係モデルを理解し、第 1〜第 3 正規形への分解ができる',
    minutes: 25,
    body: `
# ざっくり言うと

表計算ソフトで住所録を作ったことがあれば、こんな面倒に遭ったはずです。同じ会社名を何十行にも書いていて、社名が変わったときに全部直さないといけない。1 行だけ直し忘れて、古い社名が残ってしまう。

**この「同じことを何か所にも書いてあるせいで起きる事故」をなくすのが、この節のテーマ**です。方法は単純で、**重複しているものを別の表に切り出し、番号でつなぐ**。社名は会社表に 1 回だけ書き、住所録には会社番号だけ持たせる。こうすれば社名変更は 1 か所直せば済みます。

この作業を **正規化** といいます。難しそうな名前ですが、やっていることは「表を分けて、番号でつなぐ」だけです。

前半では、その前提となる用語（主キー、外部キーなど）を整理します。**主キーは「行を 1 つに特定できる列」**、つまり社員番号や商品コードのことだと考えてください。

# データベースの基礎

**DBMS**（データベース管理システム）は、データの一元管理・同時実行制御・障害回復・機密保護を担います。

3 層スキーマで独立性を確保します。

| スキーマ | 内容 |
|---|---|
| 外部スキーマ | 利用者から見たビュー |
| 概念スキーマ | データ全体の論理構造（表の定義） |
| 内部スキーマ | 物理的な格納方法 |

\`\`\`diagram:stack
title: 3 層スキーマ
top: 利用者に近い
bottom: 記憶装置に近い
外部スキーマ :: 業務ごとに必要な部分だけを見せる（ビュー）
概念スキーマ :: 表・列・制約という論理構造
内部スキーマ :: ファイル編成・索引・格納位置
note: 内部スキーマを変えても概念スキーマが変わらないのが「物理データ独立性」、概念スキーマを変えても外部スキーマが変わらないのが「論理データ独立性」です。
\`\`\`

# E-R 図

実体（エンティティ）と関連（リレーションシップ）でデータ構造を表します。関連には **1 対 1、1 対多、多対多** があり、**多対多の関連は中間の表（連関エンティティ）に分解** して実装します。

例）「学生」と「科目」は多対多 → 「履修」表を作り、学生 1 対多 履修、科目 1 対多 履修 とする。

\`\`\`diagram:flow
title: 多対多の関連を実装できる形に分解する
学生 :: 1
学生 1 対多 履修 :: 誰が履修したか
履修（連関エンティティ） :: 主キーは 学生番号 ＋ 科目コード
科目 1 対多 履修 :: 何を履修したか
科目 :: 1
note: 多対多のままでは外部キーを置く場所がありません。間に表を 1 つ挟むと、必ず「1 対多」2 本になります。
\`\`\`

# 関係モデルの用語

| 用語 | 意味 |
|---|---|
| 関係（テーブル） | 行と列からなる表 |
| **主キー** | 行を一意に識別する列。**NULL 不可・重複不可** |
| 候補キー | 主キーになりうる列の組 |
| 複合キー | 複数列を組み合わせた主キー |
| **外部キー** | 他の表の主キーを参照する列 |
| **参照制約** | 外部キーの値は参照先に存在しなければならない |

表に出てくる語を、日常の言葉に置き換えておきます。

- **主キー**：その 1 行を他と区別できる列。社員番号、注文番号など。同じ値が 2 つあってはならず、空欄も許されない
- **外部キー**：他の表を指し示す列。住所録の「会社番号」がこれで、会社表の主キーを指している
- **参照制約**：外部キーが指す先が実在していなければならない、という決まり。存在しない会社番号は書けない
- **候補キー**：主キーになれる資格のある列。社員番号でもマイナンバーでも一意に特定できるなら、両方が候補キー

# 正規化

データの重複と更新時異状（更新時に矛盾が生じること）をなくすための分解手順です。

\`\`\`diagram:flow
title: 正規化の 3 段階
非正規形 :: 1 つのセルに繰返し項目がある
第 1 正規形 :: 繰返し項目を排除
第 2 正規形 :: 部分関数従属を排除
第 3 正規形 :: 推移関数従属を排除
note: 「1 は繰返し、2 は部分、3 は推移」。この 3 語を順に唱えられれば、正規形の判定問題は落としません。
\`\`\`

## 非正規形 → 第 1 正規形

**繰返し項目をなくす**（1 つのセルに 1 つの値）。

\`\`\`
非正規形： 伝票番号 | 日付 | {商品コード, 数量} が繰り返し
第1正規形： 伝票番号 | 日付 | 商品コード | 数量
\`\`\`

正規化は 3 段階ありますが、**どれも「1 つの表に、性質の違うものを混ぜない」**という同じ方針の繰り返しです。

- **第 1 正規形**：1 つのマスに複数の値を詰め込まない。「商品 A、商品 B、商品 C」と 1 マスに書かず、行を分ける
- **第 2 正規形**：主キーの**一部**だけで決まる項目を、別の表に出す
- **第 3 正規形**：主キーではなく**他の列**によって決まる項目を、別の表に出す

第 2 と第 3 の違いは「**何によって決まっているか**」だけです。主キーの一部で決まるなら第 2 段階、主キー以外の列で決まるなら第 3 段階の対象になります。

## 第 1 → 第 2 正規形

**部分関数従属をなくす**。主キーの一部だけで決まる項目を別表に分ける。

\`\`\`
主キー = {伝票番号, 商品コード} のとき
  商品名 は 商品コード だけで決まる（部分関数従属）
→ 商品表（商品コード, 商品名, 単価）に分離
\`\`\`

## 第 2 → 第 3 正規形

**推移関数従属をなくす**。主キー以外の項目によって決まる項目を別表に分ける。

\`\`\`
伝票表（伝票番号, 日付, 顧客コード, 顧客名）
  顧客名 は 顧客コード によって決まる（推移関数従属）
→ 顧客表（顧客コード, 顧客名）に分離
\`\`\`

> **試験のポイント** 「第 2 正規形は部分関数従属の排除」「第 3 正規形は推移関数従属の排除」。この対応は必ず問われます。正規化を進めると更新時異状は減りますが、**結合が増えて検索性能は落ちる** というトレードオフも押さえます。

## 関数従属の記号

\`A → B\` は「A が決まれば B が一意に定まる」という意味です。正規化の問題は、まずこの矢印をすべて書き出すところから始めます。

> **よくある勘違い** 「正規化はやればやるほど良い」わけではありません。表を分ければ分けるほど、データを取り出すときに**表をつなぎ直す作業（結合）が増えて遅くなります**。更新時の事故は減るが検索は遅くなる、という交換条件です。この双方向の関係が試験で問われます。

# インデックス

検索を高速化する索引です。**B 木（B+ 木）インデックス** が一般的で、範囲検索にも強いのが特徴です。

- 更新が多い列に張ると更新コストが増える
- カーディナリティ（値の種類）が低い列（性別など）では効果が薄い

# この節のまとめ

- 正規化とは「**重複を別の表に切り出し、番号でつなぐ**」こと。同じ情報を何か所にも書かないための工夫
- 主キーは行を 1 つに特定できる列。**重複も空欄も許されない**
- 外部キーは他の表を指す列。指す先が実在しなければならない（参照制約）
- 第 2 正規形は「主キーの一部で決まる項目」を、第 3 正規形は「主キー以外の列で決まる項目」を切り出す
- **正規化を進めると更新の事故は減るが、結合が増えて検索は遅くなる**

# 関連する節と練習

- [UML と設計技法](textbook/t-dev-3)　E-R 図と UML
- [インデックスと性能設計](textbook/t-db-4)　設計と性能のトレードオフ
`,
  },
  {
    id: 't-db-2',
    categoryId: 't-db',
    title: 'SQL と関係演算',
    goal: '選択・射影・結合の意味を理解し、基本的な SQL を読み書きできる',
    minutes: 25,
    body: `
# ざっくり言うと

データベースから欲しいデータを取り出す操作は、突き詰めると **3 種類しかありません**。

**行を絞る**（東京都の人だけ）、**列を選ぶ**（名前と電話番号だけ）、**表をつなぐ**（住所録と会社表を会社番号で合わせる）。この 3 つです。

そして SQL は、この 3 つを文章にしたものにすぎません。SELECT のあとに書くのが「選ぶ列」、FROM が「使う表」、WHERE が「絞る条件」。**英語の語順そのまま**なので、プログラミング経験がなくても読めます。

この節で戸惑いやすいのは、グループごとの集計です。「部署ごとの平均給与」のように、行をまとめてから計算する場面では、**まとめる前に絞るのか、まとめた後に絞るのか**で書く場所が変わります。ここだけは意識して読んでください。

# 関係演算

この 3 つの操作に付けられた正式な名前が、関係演算です。

| 演算 | 内容 |
|---|---|
| **選択（selection）** | 条件に合う **行** を取り出す（WHERE） |
| **射影（projection）** | 指定した **列** を取り出す（SELECT の列指定） |
| **結合（join）** | 複数の表を共通の列で連結する |
| 和・差・積 | 集合演算（UNION / EXCEPT / INTERSECT） |

\`\`\`diagram:compare
title: 選択と射影はどちらを切り取るか
actors: 選択（selection） | 射影（projection）
取り出す方向は「横」 :: 条件に合う行だけを残す
取り出す方向は「縦」 :: 必要な列だけを残す
SQL では WHERE 句 :: WHERE 部門コード = '001'
SQL では SELECT の列指定 :: SELECT 社員番号, 氏名
列の構成は元の表と同じ :: 行数が減る
行数は元の表と同じ :: 列数が減る
note: 「選択の"択"は行を択ぶ、射影は列を影として写し取る」と結び付けて覚えると混ざりません。
\`\`\`

> **試験のポイント** 「選択 = 行、射影 = 列」。この 2 語の取り違えが定番の失点です。

# SELECT の基本形

\`\`\`sql
SELECT 列リスト
  FROM 表名
 WHERE 行の条件
 GROUP BY グループ化する列
HAVING グループへの条件
 ORDER BY 並べ替える列 [ASC | DESC]
\`\`\`

実行順序は **FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY** です。この順序を知っていると、SELECT で付けた別名を WHERE で使えない理由が理解できます。

\`\`\`diagram:flow
title: SELECT 文が評価される順序
FROM :: 対象の表（結合結果）を用意
WHERE :: 行を絞り込む
GROUP BY :: 残った行をグループにまとめる
HAVING :: グループを絞り込む
SELECT :: 列を選び別名を付ける
ORDER BY :: 最後に並べ替える
note: 別名を付けるのは SELECT の段階。だから WHERE では別名が使えず、ORDER BY では使えます。
\`\`\`

SELECT 文は英語の語順で読めます。「**何を**（SELECT）、**どこから**（FROM）、**どういう条件で**（WHERE）取り出すか」。この 3 語の順に書くだけです。並べ替えたければ最後に ORDER BY を足します。

## 集約関数

| 関数 | 意味 |
|---|---|
| COUNT(*) | 行数（NULL を含む） |
| COUNT(列) | **NULL を除いた** 件数 |
| SUM / AVG | 合計・平均（NULL は無視） |
| MAX / MIN | 最大・最小 |

> **試験のポイント** WHERE は **グループ化前の行** に対する条件、HAVING は **グループ化後** の条件。「合計が 1000 以上のグループ」は HAVING でしか書けません。

\`\`\`sql
SELECT 部門コード, COUNT(*) AS 人数, AVG(給与) AS 平均給与
  FROM 社員
 WHERE 入社年 >= 2020        -- 行の絞り込み
 GROUP BY 部門コード
HAVING COUNT(*) >= 5          -- グループの絞り込み
 ORDER BY 平均給与 DESC
\`\`\`

> **よくある勘違い** WHERE と HAVING はどちらも「絞る」ので混同しがちですが、**タイミングが違います**。WHERE は**まとめる前の 1 行ずつ**に対する条件、HAVING は**まとめた後のグループ**に対する条件です。「東京都の人だけを対象に」は WHERE、「合計が 1000 以上のグループだけ」は HAVING になります。

## 結合

\`\`\`sql
SELECT 社員.氏名, 部門.部門名
  FROM 社員 INNER JOIN 部門
    ON 社員.部門コード = 部門.部門コード
\`\`\`

- **内部結合（INNER JOIN）**：両方に対応する行がある場合だけ出力
- **外部結合（LEFT / RIGHT OUTER JOIN）**：片方に対応がなくても残し、欠けた側は NULL

\`\`\`diagram:compare
title: 部門未配属の社員は結果に残るか
actors: 内部結合（INNER JOIN） | 左外部結合（LEFT OUTER JOIN）
両方に一致する行だけ :: 部門コードが NULL の社員は消える
左の表の行はすべて残す :: 部門名が NULL になって残る
行数は減りうる :: 一致しない行が落ちる
行数は左の表以上 :: 落ちない
「対応が取れた組合せ」を見たいとき :: 売上と商品の突合せなど
「漏れ」を見つけたいとき :: 部門未登録の社員を洗い出す
note: 「片方にしかないデータを探したい」という要件が出たら外部結合、が試験の合図です。
\`\`\`

## 副問合せ

\`\`\`sql
SELECT 氏名 FROM 社員
 WHERE 給与 > (SELECT AVG(給与) FROM 社員)
\`\`\`

**EXISTS** は「該当行が存在するか」を判定します。**IN** は値のリストとの一致を判定します。

## その他の頻出構文

| 構文 | 意味 |
|---|---|
| \`LIKE 'A%'\` | A で始まる（\`%\` は任意長、\`_\` は 1 文字） |
| \`BETWEEN a AND b\` | a 以上 b 以下 |
| \`IS NULL\` | NULL の判定（\`= NULL\` は不可） |
| \`DISTINCT\` | 重複行を除く |

結合には 2 種類あります。**内部結合**は「両方の表にちゃんと相手がいる行だけ」を出します。**外部結合**は「相手がいなくても片方は残す」やり方で、欠けた側は空欄（NULL）になります。

会員名簿と注文履歴をつなぐとき、内部結合なら注文したことのある会員だけが並び、外部結合（会員側を残す）なら注文が 0 件の会員も注文欄が空のまま残ります。**「注文していない会員も一覧に出したい」なら外部結合**、という判断になります。

# データ定義とビュー

\`\`\`sql
CREATE TABLE 社員 (
  社員番号 CHAR(6) PRIMARY KEY,
  氏名     VARCHAR(40) NOT NULL,
  部門コード CHAR(3) REFERENCES 部門(部門コード)
);

CREATE VIEW 営業部社員 AS
  SELECT * FROM 社員 WHERE 部門コード = '001';
\`\`\`

**ビュー** は実表から導出される仮想の表です。利用者に必要な範囲だけを見せることで、機密保護と操作の簡略化を実現します。

権限の付与は \`GRANT\`、取消しは \`REVOKE\` です。

# この節のまとめ

- データの取り出しは 3 種類だけ。**行を絞る（選択）、列を選ぶ（射影）、表をつなぐ（結合）**
- SELECT 文は「何を・どこから・どういう条件で」の順。英語の語順そのまま
- **WHERE はまとめる前の行に、HAVING はまとめた後のグループに**かける条件
- 内部結合は「両方に相手がいる行だけ」、外部結合は「相手がいなくても片方を残す」
- NULL の判定に等号は使えない。IS NULL と書く

# 関連する節と練習

- [データベース設計と正規化](textbook/t-db-1)　表の設計と正規化
- [情報セキュリティの基礎と脅威](textbook/t-sec-1)　SQL インジェクション
`,
  },
  {
    id: 't-db-3',
    categoryId: 't-db',
    title: 'トランザクションと障害回復',
    goal: 'ACID 特性・排他制御・ロールバック/ロールフォワードを説明できる',
    minutes: 20,
    body: `
# ざっくり言うと

銀行の振込を思い浮かべてください。**A さんの口座から 1 万円引く**、**B さんの口座に 1 万円足す**。この 2 つは、必ず両方成功するか、両方失敗するかでなければなりません。片方だけ実行されて停電したら、1 万円が消えてしまいます。

**この「まとめて成功か、まとめて取り消しか」の単位が トランザクション** です。この節は、その約束をどうやって守るかという話です。

守るべき性質が 4 つあり、頭文字をとって **ACID** と呼ばれます。全部やるか全くやらないか（原子性）、辻褄が合っていること（一貫性）、他人の作業に邪魔されないこと（独立性）、完了したら消えないこと（耐久性）。

そして「他人に邪魔されない」を実現する手段が **ロック** です。誰かが更新している間は他の人を待たせる。これも銀行の窓口と同じで、同時に同じ口座を触らせないようにする交通整理です。ただし**待たせ方を間違えると、お互いが相手を待って永久に止まる**（デッドロック）ことがあります。

# トランザクションと ACID 特性

トランザクションは「これ以上分割できない一連の処理」です。銀行振込なら「引落し」と「入金」で 1 つのトランザクションになります。

| 特性 | 意味 |
|---|---|
| **原子性（Atomicity）** | 全部実行されるか、全く実行されないかのどちらか |
| **一貫性（Consistency）** | 実行前後でデータの整合性が保たれる |
| **独立性（Isolation）** | 複数の処理が同時実行されても、逐次実行と同じ結果になる |
| **耐久性（Durability）** | コミットした結果は障害が起きても失われない |

> **試験のポイント** 4 つの頭文字と定義の対応が直接問われます。特に「原子性 = all or nothing」「独立性 = 他の処理の影響を受けない」を確実に。

処理の確定が **コミット**、取消しが **ロールバック** です。

# 排他制御

同時更新による矛盾を防ぐ仕組みです。

| ロック | 他の共有ロック | 他の専有ロック |
|---|---|---|
| **共有ロック（読取り）** | かけられる | かけられない |
| **専有ロック（更新）** | かけられない | かけられない |

\`\`\`diagram:matrix
title: 後からロックをかけられるか
x: 後から要求するロック（左：共有／右：専有）
y: すでにかかっているロック（上：共有／下：専有）
かけられる :: 読む人どうしは共存できる
かけられない :: 誰かが読んでいる間は更新できない
かけられない :: 更新中のデータは読ませない
かけられない :: 同時更新は絶対に許さない
note: 「両方とも読み取りのときだけ両立する」。4 マスのうち可なのは左上の 1 つだけです。
\`\`\`

- **専有（排他）ロック**：更新時にかける。他は読むことも書くこともできない
- **共有ロック**：参照時にかける。他の参照は許すが更新は許さない

**ロックの粒度** を細かくすると並行性は上がりますが、管理のオーバヘッドが増えます。

ロックとは、**作業中の札を立てて他人を待たせること**です。2 種類あります。

- **共有ロック**：「読んでいます」の札。他の人も読むのは構わないが、書き換えは待ってもらう
- **専有（排他）ロック**：「書き換えています」の札。他の人は読むことも書くこともできない

読むだけなら何人が同時でも問題は起きないので、共有ロックは重ねられます。**書き換えだけは 1 人ずつ**、という単純な原則です。

## デッドロック

2 つのトランザクションが互いの持つ資源を待ち合う状態です。**資源へのアクセス順序を統一する** ことで予防できます。検出した場合は片方をロールバックして解消します。

\`\`\`diagram:seq
title: デッドロックが成立するまで
actors: トランザクション 1 | トランザクション 2
トランザクション 1 -> トランザクション 2 :: T1 が表 A をロック（成功）
トランザクション 2 -> トランザクション 1 :: T2 が表 B をロック（成功）
トランザクション 1 -> トランザクション 2 :: T1 が表 B を要求 → T2 が持っているので待ち
トランザクション 2 -> トランザクション 1 :: T2 が表 A を要求 → T1 が持っているので待ち
note: 互いに相手の解放を待ち、どちらも進めなくなります。ロックを取る順序を「必ず A → B」に統一すれば起こりません。
\`\`\`

> **試験のポイント** 「表 A → 表 B」の順と「表 B → 表 A」の順でロックを取る 2 つの処理が同時に走るとデッドロック、という具体例で出題されます。

**デッドロックは、2 人が譲り合わずに固まってしまう状態**です。

A さんが席を取ってから資料を取りに行き、B さんが資料を取ってから席を取りに行く。両方が同時に動くと、A は資料待ち、B は席待ちで永久に動けません。データベースでも、表 A → 表 B の順にロックする処理と、表 B → 表 A の順にロックする処理が同時に走ると、まったく同じことが起きます。

だから対策も単純で、**全員が同じ順序でロックを取る**ようにします。

# 障害回復

DBMS は更新の前後の値を **ログ（ジャーナル）** に記録しています。

| 障害 | 回復方法 |
|---|---|
| トランザクション障害 | **ロールバック**（更新前ログで戻す） |
| システム障害（電源断など） | コミット済みは **ロールフォワード**、未コミットはロールバック |
| 媒体障害（ディスク故障） | バックアップを復元し、**更新後ログでロールフォワード** |

- **ロールバック**：更新 **前** ログを使って処理前の状態に戻す
- **ロールフォワード**：更新 **後** ログを使ってコミット済みの状態まで進める

\`\`\`diagram:compare
title: 使うログと向きが正反対
actors: ロールバック | ロールフォワード
使うのは更新前ログ :: 変更前の値が入っている
使うのは更新後ログ :: 変更後の値が入っている
時間を「戻す」 :: 処理を始める前の状態へ
時間を「進める」 :: コミットした時点の状態へ
対象は未コミットのトランザクション :: 中途半端な更新を打ち消す
対象はコミット済みのトランザクション :: 確定済みの結果を復元する
note: 「未コミットは戻す、コミット済みは進める」。どちらのログを使うかとセットで問われます。
\`\`\`

**チェックポイント** は、主記憶上のバッファ内容をディスクに書き出す時点です。回復処理はチェックポイント以降のログだけを見ればよいため、復旧時間が短縮されます。

ロールバックとロールフォワードは、**名前のとおりの方向**です。

- **ロールバック**＝巻き**戻す**。まだ完了していなかった処理を、なかったことにする。使うのは「変更前の値」の記録
- **ロールフォワード**＝巻き**進める**。完了していた処理を、記録をもとにやり直して追いつかせる。使うのは「変更後の値」の記録

停電から復旧したとき、完了していた処理は進めて反映し、途中だった処理は戻して消す。**両方を同時に行って辻褄を合わせます。**

# 分散データベースと NoSQL

- **2 相コミット**：分散環境で、全サイトに「コミット可能か」を問い合わせ（第 1 相）、全て可なら確定を指示する（第 2 相）
- **レプリケーション**：複製を別サイトに持ち、可用性と参照性能を高める
- **NoSQL**：キーバリュー型、ドキュメント型、列指向、グラフ型。大量データや柔軟なスキーマに向く
- **データウェアハウス / データマイニング**：蓄積した業務データを分析用に整理し、傾向や規則を発見する
- **OLAP**：多次元的にデータを集計・分析する（ドリルダウン、スライシング）

# この節のまとめ

- トランザクションは「**まとめて成功か、まとめて取り消しか**」の単位。振込のような処理には必須
- ACID のうち、原子性は「全部か、全くやらないか」、独立性は「他の処理に邪魔されない」
- ロックは作業中の札。**読むだけなら同時に何人でもよいが、書き換えは 1 人ずつ**
- デッドロックは、互いに相手のロック解除を待って止まる状態。**全員が同じ順序でロックを取れば防げる**
- **ロールバックは巻き戻す（変更前の記録を使う）、ロールフォワードは巻き進める（変更後の記録を使う）**

# 関連する節と練習

- [OS とタスク管理](textbook/t-sw-1)　OS 側の排他制御
- [システム構成と信頼性設計](textbook/t-sys-1)　障害への備え
`,
  },
  {
    id: 't-db-4',
    categoryId: 't-db',
    title: 'インデックスと性能設計',
    goal: 'B+ 木インデックスの仕組みを理解し、効くケースと効かないケースを判断できる',
    minutes: 25,
    body: `
# ざっくり言うと

本の巻末にある索引を思い浮かべてください。「正規化」という語がどのページにあるか調べたいとき、1 ページ目からめくるのではなく、索引を引いて「128 ページ」と分かる。**データベースのインデックスは、まさにこの索引です。**

索引があると検索は劇的に速くなります。では、なぜ全部の列に索引を付けないのでしょうか。**索引そのものを維持する手間がかかる**からです。本にページを 1 枚差し込むたびに索引も作り直す必要があるように、データを 1 件追加するたびに索引も更新されます。だから**更新が多い列に索引を付けると、かえって遅くなります**。

もう 1 つ、**索引が効かない場面**があります。これが試験の頻出ポイントです。索引に載っているのは「その列の値そのもの」なので、値を加工した条件では引けません。「氏名の先頭 2 文字が山田」という条件では、氏名の索引は使えないのです。

この節は理屈が中心ですが、**現場でいちばん役に立つ内容**でもあります。

# なぜインデックスで速くなるのか

インデックスがない表を検索すると、DBMS は先頭から最後まで全行を読む **全表走査（フルスキャン）** を行います。100 万行の表なら 100 万行分の読込みが必要です。

インデックスは、本の巻末索引と同じ発想です。**キーの値を並べ替えた索引** を別に持ち、そこから目的の行の格納位置を一気に特定します。

\`\`\`diagram:compare
title: 100 万行から 1 行を探す
actors: インデックスなし | インデックスあり
先頭から順に全行を調べる :: 平均で 50 万行、最悪 100 万行
索引をたどって位置を特定 :: 数回のたどりで到達
行数に比例して遅くなる :: データが増えるほど致命的
行数が増えても悪化が緩やか :: 木の高さは対数的にしか増えない
更新の負担は表だけ :: 追加・削除が軽い
更新のたびに索引も直す :: 追加・削除・更新が重くなる
note: インデックスは「検索を速くする代わりに、更新を遅くし、記憶領域を消費する」道具です。
\`\`\`

# B+ 木インデックスの構造

関係データベースで最も広く使われるのが **B+ 木** です。

\`\`\`diagram:tree
title: B+ 木の 3 階層
ルートノード :: 大まかな範囲を示す
  中間ノード（左） :: さらに細かい範囲へ振り分ける
    リーフノード :: 実データへのポインタを持つ
    リーフノード :: 隣のリーフと横につながっている
  中間ノード（右） :: 同じく振り分け役
    リーフノード :: 実データへのポインタを持つ
    リーフノード :: キー順に並んでいる
note: 実データの位置を持つのはリーフだけ。しかもリーフどうしが順に連結されているため、範囲検索が得意です。
\`\`\`

B+ 木の重要な性質は次の 3 点です。

- **どのリーフまでも深さが同じ**（平衡木）なので、探索時間が値によってばらつかない
- **リーフがキー順に連結** されているので、\`BETWEEN\` や \`>=\` のような **範囲検索が速い**
- 挿入・削除でノードが溢れたり空きすぎたりすると、**分割・併合** して平衡を保つ（この処理が更新コストになる）

一方、**ハッシュインデックス** は等値検索（\`= 値\`）だけが速く、範囲検索も並べ替えもできません。

> **試験のポイント** 「範囲検索にも使えるのは B+ 木、等値検索専用なのがハッシュ」。この対比が定番です。

B+ 木という構造の要点は 2 つだけです。

1. **どの値を探しても同じ深さでたどり着く**ので、探す時間が値によってばらつかない
2. **一番下の段が値の順につながっている**ので、「100 以上 200 以下」のような範囲検索が速い

ハッシュ方式は 1 点を探すのは速いのですが、値が順に並んでいないため範囲検索ができません。**範囲も扱えるのが B+ 木、1 点だけならハッシュ**、という対比で覚えます。

# カーディナリティ

**カーディナリティ** は、その列に現れる **値の種類の多さ** です。

| 列の例 | 値の種類 | カーディナリティ | インデックスの効果 |
|---|---|---|---|
| 社員番号 | 全行で異なる | 非常に高い | 非常に大きい |
| 郵便番号 | 数万種類 | 高い | 大きい |
| 都道府県 | 47 種類 | 中程度 | 条件による |
| 性別・フラグ | 2 種類 | 非常に低い | ほとんど効かない |

カーディナリティが低い列に索引を張っても、絞り込んだ結果が全体の半分も残るなら、索引をたどってから実データを取りに行く手間の分だけ **かえって遅くなる** ことがあります。DBMS が「索引があるのに使わない」という判断をするのはこのためです。

> **試験のポイント** 「性別のような列に索引を張っても効果が薄いのはなぜか」→ **値の種類が少なく、絞り込めないから**。この因果関係を説明できるようにします。

**カーディナリティとは、その列に何種類の値があるか**のことです。社員番号なら全員違うので種類は多く、性別なら 2 種類しかありません。

索引が効くのは「絞り込める」ときです。1 万人の名簿で社員番号を指定すれば 1 人に絞れますが、性別を指定しても 5000 人残ります。**5000 件も残るなら、索引を経由するより最初から全部読んだほうが速い**。これが「種類の少ない列に索引を付けても効果が薄い」理由です。

# 複合インデックス

複数の列をまとめた索引を **複合インデックス** といいます。並び順に意味があり、**先頭の列から順に使われる** のが原則です。

\`\`\`
複合インデックス（部門コード, 入社年） がある場合

○ WHERE 部門コード = '001'                    … 先頭列を使うので効く
○ WHERE 部門コード = '001' AND 入社年 = 2020  … 両方使えて最も効く
× WHERE 入社年 = 2020                          … 先頭列の指定がなく効きにくい
\`\`\`

電話帳が「姓 → 名」の順に並んでいるとき、姓が分かれば速く引けるが、名だけでは引けないのと同じ理屈です。

# 実行計画

DBMS の **オプティマイザ** は、SQL をどう実行するかの手順（**実行計画**）を、統計情報（行数、値の分布）をもとに決めます。実行計画を見れば、次のことが分かります。

- 全表走査か索引走査か
- 結合の方式（ネストループ結合、ソートマージ結合、ハッシュ結合）と結合の順序
- 見積り行数と実際の行数の差

**統計情報が古い** と、オプティマイザは誤った見積りをして遅い計画を選びます。大量のデータ投入後に統計情報を更新するのは、この誤りを防ぐためです。

# インデックスが効かない代表的なケース

| 書き方 | なぜ効かないか |
|---|---|
| \`WHERE SUBSTR(氏名,1,2) = '山田'\` | 索引に入っているのは列の値そのもの。**加工した結果** では引けない |
| \`WHERE 単価 * 1.1 > 1000\` | 同上。列を演算した形は避け、\`単価 > 1000 / 1.1\` と書き換える |
| \`WHERE 氏名 LIKE '%田%'\` | **前方一致でない** ため、並び順の利点が使えない |
| \`WHERE 部門コード <> '001'\` | 否定条件は大半の行が該当し、絞り込めない |
| \`WHERE 備考 IS NULL\` | NULL を索引に含めない実装では引けない |
| 対象行が表の大半を占める | 索引経由より全表走査の方が速いと判断される |

> **試験のポイント** 「索引列を加工すると索引が使えなくなる」「中間一致の LIKE は効かない」は繰り返し出ます。**条件式の左辺に列だけを置く** のが原則です。

ここが試験でいちばん問われる部分です。**索引が効かなくなる条件には共通点があります。列をそのままの形で条件に書いていない**、という 1 点です。

索引には「その列の値」がそのまま並んでいます。だから列を計算したり切り取ったりした結果では、索引の並びと対応が取れません。対策も単純で、**条件式の左辺には列だけを置き、計算は右辺でやる**。単価 × 1.1 > 1000 ではなく、単価 > 1000 ÷ 1.1 と書き換えます。

# 非正規化というトレードオフ

第 3 正規形まで分解すると更新時異状は消えますが、参照のたびに結合が必要になります。参照が圧倒的に多く、結合の負荷が問題になる場合、**あえて正規形を崩す** ことがあります。これが **非正規化** です。

| 手法 | 得るもの | 失うもの |
|---|---|---|
| 冗長列の保持（伝票に顧客名を持つ） | 結合が不要になり参照が速い | 顧客名変更時に両方直す必要（更新時異状の危険） |
| 集計値の保持（受注表に合計金額を持つ） | 毎回の集計計算が不要 | 明細変更時に合計の整合を保つ処理が必要 |
| 表の分割・統合 | 用途に合った入出力量にできる | 設計が複雑になり、変更に弱くなる |

非正規化は「最適化」ではなく **意図的な妥協** です。適用するのは、**正規化した設計で性能要件を満たせないことを実測で確認してから** にします。先に崩すと、原因不明のデータ不整合を抱え込むだけになります。

> **試験のポイント** 「正規化を進めると更新時異状は減るが検索性能は落ちる」「非正規化はその逆」という双方向のトレードオフが問われます。片方向だけの理解では選択肢を絞り切れません。

## バッファとヒット率

DBMS はディスクから読んだページを主記憶の **バッファプール** に置き、次に同じページを求められたらそこから返します。ディスクに行かずに済んだ割合が **ヒット率** です。実効的なアクセス時間は、キャッシュメモリとまったく同じ式で求められます。

\`\`\`
実効アクセス時間 = ヒット率 × 高速側の時間 ＋ (1 − ヒット率) × 低速側の時間
\`\`\`

数値を変えて、ヒット率が実効アクセス時間をどれだけ左右するか確かめてみましょう。

\`\`\`widget:cache
\`\`\`

# この節のまとめ

- インデックスは本の索引と同じ。**引ければ速いが、維持する手間がかかる**ので更新の多い列には不向き
- B+ 木は**どの値も同じ深さ**でたどり着き、**範囲検索にも使える**。ハッシュは 1 点検索専用
- 値の種類が少ない列（性別など）に索引を付けても、**絞り込めないので効果が薄い**
- **列を加工した条件では索引が使えない**。条件式の左辺には列だけを置く
- 中間一致の検索（前後に何か付く形）も索引が効かない

# 関連する節と練習

- [データ構造](textbook/t-algo-1)　木構造とハッシュ表
- [探索・整列アルゴリズムと計算量](textbook/t-algo-2)　計算量
`,
  },
  {
    id: 't-db-5',
    categoryId: 't-db',
    title: 'データ活用とビッグデータ基盤',
    goal: 'DWH・ETL・OLAP・NoSQL の役割を区別し、データ活用の流れを説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

お店のレジは「1 件の会計を、正確に、速く」処理します。一方で経営者は「先月の売上を店舗別・商品別に見たい」と考えます。**この 2 つは求めるものが正反対**です。

前者は 1 件ずつの登録と更新が中心で、間違いが起きないよう重複を徹底的に排除した作りが向いています。後者は何百万件をまとめて集計するので、多少重複していても**集計しやすい形**のほうが向いています。

そこで、**日々の処理をするデータベースとは別に、分析専用のデータベースを用意する**という発想が生まれます。それが **データウェアハウス**（データの倉庫）です。

この節では、業務データを分析用に運び込む流れ（ETL）、集計の切り口を変えながら見る操作（OLAP）、そして表の形にこだわらない新しい種類のデータベース（NoSQL）を扱います。用語は多いですが、**どれも「大量のデータをどう活かすか」という 1 つの目的につながっています。**

# 業務データベースと分析用データベース

同じ「データベース」でも、日々の業務を回すためのものと、分析するためのものでは設計思想が正反対です。

| | 業務系（OLTP） | 分析系（OLAP） |
|---|---|---|
| 目的 | 受注・入金などの **処理** | 傾向の把握・意思決定の **分析** |
| 代表的な操作 | 1 件の登録・更新 | 大量データの集計 |
| 求められる性能 | 短い応答時間、高い同時実行性 | 大量読取りのスループット |
| データの鮮度 | 常に最新 | 一定周期で取り込んだ時点のもの |
| 設計 | 正規化して重複を排除 | **非正規化**（スタースキーマ）で結合を減らす |

業務系のデータベースで重い集計を走らせると、本来の業務処理が遅くなります。だから分析用に別の器を用意します。それが **データウェアハウス（DWH）** です。

# データウェアハウスとデータマート

**データウェアハウス** は、複数の業務システムから集めたデータを、分析しやすい形に整えて **時系列で蓄積** する倉庫です。特徴は「**主題ごとに整理され、統合されており、時系列で保持され、更新されない（消さずに積み上げる）**」ことです。

**データマート** は、DWH から特定の部門や用途に必要な部分だけを切り出した小さな倉庫です。営業部門用、在庫分析用のように分けることで、利用者が扱う量を減らし、応答を速くします。

\`\`\`diagram:flow
title: 業務データが意思決定に届くまで
業務システム :: 販売・会計・在庫など個別のデータベース
ETL :: 抽出・変換・ロード
データウェアハウス :: 全社分を時系列で蓄積
データマート :: 部門・用途ごとに切り出す
BI ツール / OLAP :: 集計・可視化して分析する
意思決定 :: 施策の立案と実行
note: 上流のデータが汚れていると、どんなに優れた分析ツールを使っても誤った結論しか出ません。
\`\`\`

## ETL

DWH に取り込む前処理を **ETL** と呼びます。

| 工程 | 内容 |
|---|---|
| **E：抽出（Extract）** | 各業務システムから必要なデータを取り出す |
| **T：変換（Transform）** | 表記ゆれの統一、コード体系の統合、単位換算、重複排除、欠損値の処理 |
| **L：ロード（Load）** | 変換したデータを DWH に格納する |

もっとも手間がかかるのは変換です。「株式会社 A」「（株）A」「A 株式会社」が同一企業だと機械には分かりません。この統一を **名寄せ** といいます。

先に生データを格納してから必要時に変換する **ELT** という順序もあり、これはデータレイクの考え方と相性がよい方式です。

**ETL は、業務システムから分析用の倉庫へデータを運び込む作業**です。3 つの頭文字がそのまま工程を表します。

- **E（抽出）**：各システムから必要なデータを取り出す
- **T（変換）**：表記のゆれを揃える。「株式会社ABC」と「(株)ABC」を統一する、日付の形式を揃える、といった地味だが重要な作業
- **L（ロード）**：整えたデータを倉庫に格納する

実務では **T（変換）にいちばん手間がかかります**。システムごとにコードの付け方や単位が違うためです。

# OLAP による多次元分析

売上を「商品 × 地域 × 期間」のような複数の軸（**ディメンション**）で捉え、切り口を変えながら見ていく分析手法です。

| 操作 | 内容 |
|---|---|
| **ドリルダウン** | 集計の粒度を **細かく** する（年 → 四半期 → 月 → 日） |
| **ドリルアップ（ロールアップ）** | 粒度を **粗く** する（日 → 月 → 年） |
| **スライシング** | 1 つの軸を固定して断面を見る（「2025 年 4 月だけ」の商品 × 地域） |
| **ダイシング** | 軸の組合せを入れ替えて別の角度から見る |

\`\`\`diagram:cycle
title: OLAP で原因にたどり着く流れ
全社の売上が前年割れ :: まず粗い粒度で異常に気付く
地域でスライス :: 関西だけが落ちていると分かる
商品カテゴリへドリルダウン :: 特定カテゴリの不振と判明
月次・週次へさらに掘る :: 落ち始めた時期を特定する
note: 「異常に気付く → 軸を絞る → 粒度を細かくする」を繰り返して原因に迫るのが OLAP の使い方です。
\`\`\`

> **試験のポイント** **ドリルダウン＝より詳細へ** の向きを取り違えないこと。「年間売上から月別売上へ掘り下げる」がドリルダウンです。

OLAP の 4 つの操作は、**地図アプリの操作**にたとえると分かります。

- **ドリルダウン**＝拡大。年間の売上から、月別、日別へと**細かく**していく
- **ドリルアップ**＝縮小。日別から月別、年別へと**粗く**していく
- **スライシング**＝1 つの軸を固定して断面を見る。「2025 年 4 月だけ」に限定して、商品と地域の表を見る
- **ダイシング**＝軸を入れ替えて別角度から見る

> **よくある勘違い** ドリルダウンの向きを取り違える人が多くいます。ドリル（穴を掘る道具）で**下へ掘り進める**イメージなので、**より詳細へ**が正解です。

# データマイニング

蓄積したデータから、**人が気付いていなかった規則性や関連** を統計・機械学習の手法で見つけ出すことです。代表例が **バスケット分析（アソシエーション分析）** で、「商品 A を買った人は商品 B も買う」という併売の関係を見つけ、棚割やレコメンドに使います。

OLAP が「**仮説を持った人が確かめる**」のに対し、データマイニングは「**仮説そのものを機械に探させる**」点が異なります。

**テキストマイニング** は、問合せ記録やアンケートの自由記述など、非構造データから傾向を抽出する手法です。

# NoSQL の 4 分類

関係データベースの表形式や結合、厳密な整合性にこだわらず、大量データや柔軟な構造への対応を優先したデータベース群です。

\`\`\`diagram:tree
title: NoSQL の 4 分類
NoSQL :: 関係モデル以外のデータベース
  キーバリュー型 :: キーと値の対だけ。最も単純で最も速い
  ドキュメント指向型 :: JSON 等の構造をそのまま格納。スキーマが柔軟
  列指向型（カラム型） :: 列単位で格納。特定列の大量集計に強い
  グラフ型 :: 節点と枝で関係を表す。つながりの探索に強い
note: 「何を速くしたいか」で選びます。SNS の友人関係の探索ならグラフ型、セションの保持ならキーバリュー型です。
\`\`\`

| 分類 | 向いている用途 |
|---|---|
| キーバリュー型 | セション管理、キャッシュ、設定値の保持 |
| ドキュメント指向型 | 項目構成が案件ごとに変わるログや商品情報 |
| 列指向型 | 巨大なテーブルの特定列だけを集計する分析処理 |
| グラフ型 | 交友関係、推薦、経路探索 |

多くの NoSQL は厳密な ACID ではなく、**一時的な不整合を許して最終的に整合させる（結果整合性）** 方針を取ります。これにより、複数サーバへ分散させて **スケールアウト** しやすくなります。

> **試験のポイント** 「NoSQL は関係データベースの上位互換ではない」ことに注意。**結合や厳密なトランザクションが必要なら関係データベース** が適切です。

# データレイクとデータ品質

**データレイク** は、構造化・非構造化を問わず、**生のまま** 大量に蓄える保管場所です。DWH が「使う形に整えてから入れる」のに対し、データレイクは「まず入れて、使うときに整える」考え方です。

\`\`\`diagram:compare
title: 整えてから入れるか、入れてから整えるか
actors: データウェアハウス | データレイク
先に変換してから格納（ETL） :: 使う目的が決まっている
生のまま格納し後で変換（ELT） :: 目的が後から決まってもよい
構造化データが中心 :: 表形式に整えたもの
画像・音声・ログも保持 :: 非構造データを含む
用途が明確で分析が速い :: 分析者がすぐ使える
整備を怠ると使えなくなる :: 「データスワンプ（沼）」化する
note: データレイクは自由度が高い代わりに、カタログと品質管理を怠ると誰も使えないゴミ置き場になります。
\`\`\`

## データの品質とマスタ管理

分析の価値は、元データの品質を超えられません。品質は次の観点で評価します。

- **正確性**：値が事実と一致しているか
- **完全性**：必要な項目が欠けていないか
- **一貫性**：システム間で同じ意味の値が食い違っていないか
- **適時性**：意思決定に間に合う鮮度か
- **一意性**：同じ実体が重複して登録されていないか

顧客・商品・組織のように **複数の業務システムが共通で使う基準データ** を **マスタデータ** と呼び、その正しさを全社で一元的に維持する取組みを **MDM（マスタデータ管理）** といいます。部門ごとに別の顧客コードを持っていると、統合した瞬間に集計が合わなくなります。分析基盤の成否は、実は分析手法よりもマスタ整備で決まります。

**データサイエンティスト** はこれらのデータから価値を引き出す専門人材で、統計・情報技術・業務知識の 3 つを併せ持つことが求められます。

# この節のまとめ

- 日々の処理用（正確さ重視）と分析用（集計しやすさ重視）では、**求められる作りが正反対**
- 分析専用に用意したデータベースが **データウェアハウス**
- **ETL は抽出・変換・格納**。いちばん手間がかかるのは表記ゆれを揃える変換
- **ドリルダウンは詳細へ掘り下げる**、ドリルアップは粗くまとめる
- NoSQL は表の形にこだわらない種類のデータベース。**結合や厳密なトランザクションが必要なら関係データベースのほうが適切**

# 関連する節と練習

- [DX とデータ活用戦略](textbook/s-sys-3)　データドリブン経営
- [AI・IoT と先端技術](textbook/s-biz-3)　AI とデータ分析
`,
  },
];
