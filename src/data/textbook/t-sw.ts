import type { TextbookSection } from '../../types';

export const tSw: TextbookSection[] = [
  {
    id: 't-sw-1',
    categoryId: 't-sw',
    title: 'OS とタスク管理',
    goal: 'OS の役割、タスクの状態遷移、スケジューリング方式を説明できる',
    minutes: 20,
    body: `
# ざっくり言うと

パソコンでは、音楽を聴きながらブラウザを開き、裏で更新プログラムが走っています。でも CPU は 1 つしかありません。**では、どうやって同時に動いているように見せているのでしょうか。**

答えは「**ものすごく速く切り替えている**」です。音楽を少し、ブラウザを少し、更新を少し。1 秒間に何百回も持ち回りで切り替えるので、人間には同時に動いているように見えます。

この切り替えを取り仕切っているのが **OS** です。誰にどれだけ CPU を使わせるか、メモリのどこを誰に貸すか、ファイルを誰に触らせるか——**限られた資源の交通整理**が OS の仕事のすべてです。

この節では、その中でも「CPU をどう配るか」（タスク管理）を扱います。順番待ちの行列をどうさばくか、という話なので、**銀行の窓口や病院の待合室を思い浮かべる**とほぼそのまま当てはまります。

# OS の役割

OS はハードウェアと応用ソフトウェアの仲立ちをし、次の資源を管理します。

- タスク（プロセス）管理
- 記憶管理
- ファイル管理
- 入出力管理
- 利用者管理・セキュリティ管理

**カーネル** が中核部分で、応用プログラムは **API（システムコール）** を通じて OS の機能を呼び出します。

\`\`\`diagram:stack
title: OS を挟んだソフトウェアの階層
top: 利用者に近い
bottom: ハードウェアに近い
応用ソフトウェア :: 業務アプリケーション
ミドルウェア :: DBMS・Web サーバなど
API（システムコール） :: OS の機能を呼び出す窓口
カーネル :: タスク・記憶・入出力を管理
ハードウェア :: CPU・主記憶・周辺装置
note: 応用ソフトウェアがハードウェアを直接触らずに済むのは、間に OS があるから。この「仲立ち」が OS の本質です。
\`\`\`

# タスクの状態遷移

\`\`\`
       実行可能状態 ──ディスパッチ──> 実行状態
            ↑                            │
     　　   │<──タイムアウト/優先度──────┘
            │                            │入出力要求
        入出力完了                       ↓
            └────────────────  待機状態
\`\`\`

| 状態 | 意味 |
|---|---|
| 実行可能（レディ） | CPU が割り当てられれば動ける |
| 実行 | CPU を使って処理中 |
| 待機（ウェイト） | 入出力完了などを待っている |

- **ディスパッチ**：実行可能状態のタスクに CPU を割り当てること
- **プリエンプション**：実行中のタスクから CPU を強制的に取り上げること

\`\`\`diagram:cycle
title: タスクが巡る 3 つの状態
実行可能状態 :: CPU の割当てを待っている
実行状態 :: ディスパッチされ CPU を使用中
待機状態 :: 入出力の完了を待っている
note: 入出力が完了すると待機状態から戻るのは「実行可能状態」。実行状態へ直接は戻れません。
\`\`\`

> **試験のポイント** 「待機状態から直接実行状態には遷移しない」（必ず実行可能状態を経由する）が頻出の引っかけです。

タスクには 3 つの状態しかありません。**病院の待合室**にたとえると分かりやすくなります。

- **実行可能（レディ）**：待合室で名前を呼ばれるのを待っている。準備はできている
- **実行**：診察室に入って診てもらっている（CPU を使っている）
- **待機（ウェイト）**：検査に回されて結果を待っている。呼ばれても今は診察できない

> **よくある勘違い** 検査が終わった人がそのまま診察室に直行することはなく、いったん待合室に戻って順番を待ちます。同じように、**待機状態から直接実行状態へは移れません。必ず実行可能状態を経由します。**

# スケジューリング方式

| 方式 | 内容 |
|---|---|
| 到着順（FCFS） | 到着した順に実行。単純だが長い処理が後続を待たせる |
| **ラウンドロビン** | 一定時間（タイムクウォンタム）ごとに切り替える。対話型向き |
| 優先度順 | 優先度の高いタスクを先に実行。低優先度が実行されない **スタベーション** が起こりうる |
| 処理時間順（SPT） | 短い処理を優先。平均待ち時間が最小 |
| **エージング** | 待ち時間に応じて優先度を上げ、スタベーションを防ぐ |

方式の違いは、**窓口の呼び方の違い**です。到着順は来た順にさばく（前の人の用件が長いと後ろが詰まる）。ラウンドロビンは一定時間で区切って次の人に回す（全員がそこそこ早く反応してもらえる）。優先度順は急ぐ人を先に通す（ただし急ぎでない人がいつまでも呼ばれない恐れがある）。

その「いつまでも呼ばれない」状態を **スタベーション**（飢餓）といい、対策として**待たされた時間に応じて優先度を上げていく**のが **エージング** です。

# マルチプログラミングと排他制御

複数タスクが同じ資源を使うとき、**セマフォ** や **排他制御（ロック）** で整合性を保ちます。

**デッドロック** は、互いに相手が持つ資源を待ち続けて処理が進まなくなる状態です。発生条件（相互排除・保持と待機・横取り不可・循環待ち）のいずれかを崩せば防げます。実務では **資源の獲得順序を統一する** のが定石です。

## スレッド

プロセス内で並行実行される処理単位です。プロセスと違ってメモリ空間を共有するため、切替えが軽く通信も容易ですが、共有データの競合には注意が必要です。

# 言語処理系

| 種類 | 動作 |
|---|---|
| コンパイラ | ソースを一括して機械語に翻訳してから実行。実行が速い |
| インタプリタ | 1 行ずつ解釈しながら実行。デバッグしやすい |
| アセンブラ | アセンブリ言語を機械語に変換 |
| ジェネレータ | パラメタからプログラムを生成 |

コンパイルの流れは **字句解析 → 構文解析 → 意味解析 → 最適化 → コード生成**。生成された目的プログラムは **リンカ** で結合され、**ローダ** が主記憶に読み込みます。

\`\`\`diagram:flow
title: ソースプログラムが動き出すまで
字句解析 :: 文字列を単語（トークン）に分ける
構文解析 :: 文法どおりかを調べ構文木を作る
意味解析 :: 型の不一致などを検出する
最適化 :: 実行が速くなるよう変形する
コード生成 :: 目的プログラム（機械語）を出力
リンカ :: ライブラリと結合して実行形式に
ローダ :: 主記憶へ読み込んで実行開始
note: 「変数名の綴り誤り」は字句・構文の段階、「型の食い違い」は意味解析の段階で見つかります。
\`\`\`

- **静的リンク**：実行前にライブラリを結合
- **動的リンク（DLL）**：実行時に結合。メモリ効率がよく更新も容易

# この節のまとめ

- CPU は 1 つしかないので、OS が**高速に切り替えて**同時に動いているように見せている
- OS の仕事は、CPU・メモリ・ファイル・入出力という**限られた資源の交通整理**
- タスクの状態は 3 つ。**待機からいきなり実行には移れず、必ず実行可能を経由する**
- ラウンドロビンは一定時間で区切る方式で、対話型の処理に向く
- 優先度順は**低優先度が永久に待たされる**恐れがあり、対策がエージング
- コンパイラは一括翻訳してから実行（速い）、インタプリタは 1 行ずつ解釈（直しやすい）

# 関連する節と練習

- [プロセッサ（CPU）](textbook/t-hw-1)　割込みを出す側の CPU
- [データ構造](textbook/t-algo-1)　キューと待ち行列
`,
  },
  {
    id: 't-sw-2',
    categoryId: 't-sw',
    title: '記憶管理とファイル',
    goal: '仮想記憶とページング、ページ置換えアルゴリズム、ファイル管理を説明できる',
    minutes: 20,
    body: `
# ざっくり言うと

メモリは高価なので、たくさんは積めません。ところが、開いておきたいアプリは次々に増えていく。**足りないメモリで、どうやってやりくりするか**——これがこの節のテーマです。

やり方は意外と単純です。**いま使っていない部分を、こっそりディスクへ追い出しておく**。そして必要になったら呼び戻す。これを OS が自動でやってくれるので、利用者からは「メモリが実際より広い」ように見えます。この仕掛けが **仮想記憶** です。

机の上が狭いとき、当面使わない資料を引き出しにしまい、必要になったら出してくる。あの動作をコンピュータが勝手にやっている、と考えてください。

ただし呼び戻しには時間がかかるので、**追い出す相手を間違えると遅くなります**。「すぐまた使うものを追い出してしまった」が最悪です。だから「どれを追い出すか」の判断方法（ページ置換えアルゴリズム）が重要になり、試験でもここが必ず問われます。

# 実記憶管理

主記憶を効率よく使うための方式です。

- **固定区画方式**：あらかじめ決めた大きさに区切る。単純だが無駄が出る
- **可変区画方式**：必要な大きさだけ割り当てる。**フラグメンテーション（断片化）** が発生する
- **スワッピング**：優先度の低いタスクを丸ごと補助記憶に退避する
- **オーバレイ**：プログラムを分割し、必要な部分だけを主記憶に読み込む

断片化の解消には、使用中領域を詰め直す **コンパクション（ガーベジコレクション）** を行います。

# 仮想記憶

補助記憶を使って、主記憶より大きなアドレス空間を利用者に見せる仕組みです。

- **ページング方式**：固定長の **ページ** 単位で管理する（主流）
- **セグメント方式**：論理的なまとまり単位で管理する

仮想アドレスと実アドレスの対応は **ページテーブル** が保持し、**動的アドレス変換（DAT）** が変換します。

用語を先に補っておきます。**ページ**とは、メモリを一定の大きさに切り分けた 1 枚のことです。追い出したり呼び戻したりを 1 バイト単位でやると管理が大変なので、決まった大きさのまとまりで扱います。

そして、**必要なページがメモリになくて、ディスクから読み込む必要が生じること**を **ページフォールト** といいます。エラーのような名前ですが故障ではなく、仮想記憶では日常的に起きる正常な動作です。ただし遅いので、回数は少ないほどよい。

## ページフォールトとページ置換え

参照したページが主記憶にないと **ページフォールト**（内部割込み）が発生し、補助記憶から読み込みます。主記憶が満杯なら、どのページを追い出すかを決める必要があります。

\`\`\`diagram:flow
title: ページフォールト発生時の処理
仮想アドレスを参照 :: プログラムが命令を実行
ページテーブルを引く :: 主記憶にあるか調べる
ページフォールト :: なければ内部割込みが発生
置換えページを決定 :: FIFO / LRU / LFU
補助記憶から読込み :: 空きがなければ追い出してから
命令を再実行 :: ここでようやく処理が進む
note: 補助記憶の読込みは主記憶より桁違いに遅いため、ページフォールトの回数が性能を決めます。
\`\`\`

| アルゴリズム | 追い出す対象 |
|---|---|
| **FIFO** | 最も古く読み込んだページ |
| **LRU** | 最も長く参照されていないページ |
| **LFU** | 参照回数が最も少ないページ |

> **試験のポイント** ページ参照列を与えて「ページフォールトの回数」を数えさせる問題が定番です。**主記憶の枠を表にして 1 参照ずつ埋める** のが確実です。

参照列と枠数を変えて、FIFO と LRU で結果がどう変わるかを実際に動かして確かめてみましょう。

\`\`\`widget:page
\`\`\`

**スラッシング** は、ページの入れ替えが多発して処理がほとんど進まなくなる現象です。多重度を下げるか、実記憶を増やして解消します。

> **よくある勘違い** 「LRU は参照回数が少ないものを追い出す」と覚えるのは誤りです。LRU が見るのは**回数ではなく、最後に使われてからの時間**で、いちばん長く放置されているものを追い出します。回数で判断するのは LFU のほうです。名前で区別するなら、LRU の R は Recently（最近）、LFU の F は Frequently（頻繁に）です。

# ファイル管理

## ディレクトリとパス

- **絶対パス**：ルート（\`/\`）からの経路
- **相対パス**：カレントディレクトリからの経路。\`.\` は自分、\`..\` は親

\`\`\`
カレントが /home/user/doc のとき
../img/a.png  →  /home/user/img/a.png
\`\`\`

\`\`\`diagram:tree
title: ディレクトリの階層と相対パス
/ :: ルートディレクトリ
  home
    user
      doc :: ← カレントディレクトリ
        report.txt
      img
        a.png :: ../img/a.png でここに到達
note: 「..」で 1 段上（user）へ戻り、そこから img へ下りる、と声に出してたどると間違えません。
\`\`\`

> **試験のポイント** 相対パスの問題は、\`..\` で 1 階層上がる操作を紙にたどれば必ず解けます。

## ファイル編成

| 編成 | 特徴 |
|---|---|
| 順編成 | 先頭から順に格納。順アクセスのみ |
| 直接編成 | キーから格納位置を計算。ランダムアクセスが速い |
| 索引順編成（ISAM） | 索引で直接アクセスもできる順編成 |
| 区分編成 | メンバをまとめて格納する |

ファイル編成は「**どういう並べ方でファイルに書いておくか**」の種類です。順編成はノートに前から書いていくやり方で、読むときも前から。直接編成は「この番号の内容はここ」と計算で場所を決めるやり方で、いきなり目的の場所を開けます。索引順編成は、順に並べつつ索引（目次）も付けておく折衷案です。

## バックアップ

| 方式 | 対象 | 復旧の手間 |
|---|---|---|
| フルバックアップ | 全データ | 1 世代を戻すだけ |
| 差分バックアップ | 前回 **フル** からの変更分 | フル + 最新の差分 |
| 増分バックアップ | 前回 **バックアップ** からの変更分 | フル + すべての増分 |

\`\`\`diagram:compare
title: 月曜にフル、火〜金に取得する場合の違い
actors: 差分バックアップ | 増分バックアップ
基準は「前回のフル」 :: 月曜フル以降の変更分を毎回すべて取る
基準は「前回のバックアップ」 :: 直前の取得以降の変更分だけ取る
取得量は日ごとに増える :: 金曜が最も大きい
取得量は毎日ほぼ一定 :: 取得時間が短い
復旧はフル＋最新の差分の 2 つ :: 復旧が速い
復旧はフル＋月〜金すべての増分 :: 復旧に手間がかかる
note: 「取得が楽なら復旧が大変、取得が大変なら復旧が楽」という裏返しの関係で覚えます。
\`\`\`

差分と増分は名前が紛らわしいので、**基準がどこか**で区別してください。

- **差分**：いつも「**前回のフルバックアップ**」からの変更分を取る。日が経つほど量が増えるが、復元はフル＋最新の差分の 2 つで済む
- **増分**：「**前回のバックアップ**」からの変更分だけを取る。毎回の量は少ないが、復元にはフル＋その後の増分を全部順に当てる必要がある

**取るのが楽なのは増分、戻すのが楽なのは差分**。この一言で覚えられます。

> **試験のポイント** 差分は「バックアップ時間が長く、復旧が速い」、増分は「バックアップ時間が短く、復旧が遅い」。このトレードオフが問われます。

# この節のまとめ

- 仮想記憶は「**使っていない部分をディスクに追い出しておく**」しくみ。メモリが実際より広く見える
- ページは、メモリを一定の大きさに切り分けた 1 枚。必要なページがなくて読み込むことを**ページフォールト**という
- **LRU は「最後に使ってから最も時間が経ったもの」**を追い出す（回数ではない）
- 絶対パスはルートから、相対パスは現在地から。点 2 つは 1 つ上の階層
- バックアップは **取るのが楽なのは増分、戻すのが楽なのは差分**

# 関連する節と練習

- 数字を変えて何度でも解く：[ページフォールト回数](drill?type=page)
- [記憶階層とキャッシュ](textbook/t-hw-2)　記憶階層のおさらい
- [インデックスと性能設計](textbook/t-db-4)　バッファとヒット率
`,
  },
  {
    id: 't-sw-3',
    categoryId: 't-sw',
    title: '開発環境とオープンソースソフトウェア',
    goal: 'IDE・バージョン管理・コンテナの役割と、OSS ライセンスの違いを説明できる',
    minutes: 25,
    body: `
# ざっくり言うと

プログラムを書く作業には、書く以外の手間がたくさん付いてきます。書いたものを動く形に変換する、動かして間違いを探す、以前の状態に戻す、他人の書いた部分と合わせる、自分のパソコンと本番のサーバで動きを揃える——。

**その手間を減らす道具の話**が、この節です。実務そのものなので、経験があれば当たり前に見えますが、初めてなら名前と役割の対応を押さえるところから始めてください。

後半の **オープンソースソフトウェア（OSS）** は少し毛色が違い、**法律に近い話**です。世の中には、無料で使えるうえに中身まで公開されているソフトウェアが大量にあります。ただし「無料 ＝ 何をしてもよい」ではありません。**使い方に条件が付いており、その条件がライセンスごとに違う**。この違いが試験で問われます。

特に GPL には「これを組み込んだ自分の製品も、同じ条件で公開しなければならない」という強い条件があり、企業では扱いに注意が必要です。

# 開発を支える道具

プログラムを書く作業は「書く → 動く形にする → 動かして確かめる」の繰り返しです。それぞれを支える道具があります。

| 道具 | 役割 |
|---|---|
| **IDE（統合開発環境）** | エディタ・コンパイラ・デバッガ・実行環境を 1 つにまとめたもの |
| **ビルドツール** | コンパイル、ライブラリの結合、成果物の生成を自動化する（Make、Maven、npm など） |
| **デバッガ** | **ブレークポイント** で実行を止め、変数の値を見ながら 1 行ずつ進める |
| **静的解析ツール** | 実行せずにソースを検査し、規約違反やバグの兆候を指摘する |
| **テスティングツール** | テストの実行と結果判定を自動化する。**カバレッジ** も測れる |
| **エミュレータ / シミュレータ** | 実機がなくても動作を再現して確認する |

> **試験のポイント** 「ブレークポイントを設定して変数の値を確認する」のは **デバッガ**、「実行せずにソースコードの問題を指摘する」のは **静的解析ツール**。動かすかどうかが区別の軸です。

\`\`\`diagram:flow
title: 現代的な開発サイクル
コードを書く :: IDE・エディタ
ビルド :: 依存ライブラリを解決して結合
自動テスト :: 単体テストを一括実行
デバッグ :: 失敗した箇所を追跡
コミット :: バージョン管理に記録
note: この 1 周を短く速く回せるほど不具合の発見が早くなります。CI はこの流れを自動で回す仕組みです。
\`\`\`

# バージョン管理

ソースコードの変更履歴を記録し、いつでも過去に戻せるようにする仕組みです。**Git** が代表例で、各開発者が完全な履歴の複製を手元に持つ **分散型** である点が特徴です。

## Git の基本的な流れ

\`\`\`diagram:seq
title: 作業ツリーからリモートリポジトリまで
actors: 作業ツリー | ステージ | ローカルリポジトリ | リモートリポジトリ
作業ツリー -> ステージ :: git add（記録したい変更を選ぶ）
ステージ -> ローカルリポジトリ :: git commit（1 つの変更として履歴に残す）
ローカルリポジトリ -> リモートリポジトリ :: git push（共有サーバへ反映）
リモートリポジトリ -> 作業ツリー :: git pull（他人の変更を取り込む）
note: コミットは「意味のある 1 単位」でまとめます。何をしたか 1 行で説明できない粒度は大きすぎます。
\`\`\`

## ブランチとマージ

**ブランチ** は履歴を枝分かれさせる仕組みです。機能追加や不具合修正ごとに枝を切って作業すれば、本流（main）を壊さずに開発を進められます。作業が終わったら本流に **マージ** して合流させます。

**コンフリクト（競合）** は、同じファイルの同じ行を 2 人が別々に変更した状態でマージしたときに発生します。ツールは自動で優劣を判断できないため、**人間がどちらを残すか決めて解消** します。

\`\`\`diagram:flow
title: コンフリクトが起きたときの手順
発生を検知 :: マージ時にツールが停止して知らせる
競合箇所を開く :: 両者の変更が印付きで並記される
どちらを残すか決める :: 必要なら両方を取り込んで書き直す
印を消して保存 :: 目印の記号を残さない
ビルドとテスト :: 壊れていないことを確認してからコミット
note: 「先に更新した人の変更を自動で捨てる」ような仕組みは存在しません。判断は必ず人が行います。
\`\`\`

> **試験のポイント** 「コンフリクトの原因」と「解決は人手で行う」ことが問われます。予防策は **こまめに pull して差分を小さく保つ**、**担当ファイルを分ける** ことです。

バージョン管理とは、**ファイルの変更履歴をすべて記録しておくしくみ**です。いつ・誰が・どこを変えたかが残るので、おかしくなったら前の状態に戻せます。

もう 1 つの大きな役割が、**複数人で同じファイルを触っても壊れないようにすること**です。とはいえ同じ行を 2 人が同時に書き換えれば、機械にはどちらが正しいか判断できません。これを **コンフリクト（競合）** といい、**解決は必ず人間が行います**。

# コンテナと開発環境

**仮想化** は 1 台の物理マシン上に複数の実行環境を作る技術です。

| 方式 | 特徴 |
|---|---|
| **仮想マシン（ホスト型・ハイパバイザ型）** | ゲスト OS を丸ごと動かす。分離は強いが起動が重い |
| **コンテナ** | OS のカーネルを共有し、アプリと必要なライブラリだけを閉じ込める。**軽量で起動が速い** |

コンテナを使うと「自分の PC では動くのに本番では動かない」という環境差の問題を減らせます。開発・試験・本番で **同じイメージ** を動かせるからです。

# オープンソースソフトウェア（OSS）

OSS は、ソースコードが公開され、利用・改変・再配布が認められたソフトウェアです。「無償であること」は OSS の定義そのものではなく、**再配布の自由が保証されていること** が本質です。

| ライセンス | 特徴 |
|---|---|
| **GPL** | **コピーレフト**。改変・組込みした派生物も同じ GPL で公開する義務がある |
| **LGPL** | ライブラリとしてリンクするだけなら自社製品を公開しなくてよい |
| **MIT** | 著作権表示さえ残せば、商用でもソース非公開でも利用できる |
| **Apache License 2.0** | MIT に近い寛容型。加えて **特許の扱い** を明記している |
| **BSD** | 寛容型。表示義務が中心 |

\`\`\`diagram:compare
title: コピーレフト型と寛容型
actors: GPL（コピーレフト型） | MIT・Apache（寛容型）
派生物も同じライセンスで公開 :: 自社コードの開示義務が生じうる
公開義務なし :: 自社製品に組み込んで販売できる
自由を「引き継がせる」思想 :: 利用者の自由を守り続ける
自由を「渡し切る」思想 :: 使い方を利用者に委ねる
社内利用のみなら公開義務は生じない :: 配布した時点で義務が発生
著作権表示の保持は必須 :: 表示を消すのは違反
note: 「GPL のコードを組み込んだ製品を配布する」場面が試験の典型例です。
\`\`\`

> **試験のポイント** GPL は「配布するときに」派生物のソース公開義務が生じます。社内で使うだけなら義務はありません。「OSS だから何をしてもよい」「OSS には著作権がない」は誤りで、**著作権は放棄されていません**。

ライセンスは、**「返す義務がどれだけあるか」で 2 つに分かれます**。

- **コピーレフト型（GPL）**：「自由に使ってよいが、これを使って作ったものも同じように自由にしなさい」という考え方。**組み込んだ自社製品のソースも公開する義務**が生じる
- **寛容型（MIT、BSD、Apache）**：「著作権表示さえ残せば、あとは好きにしてよい」。自社製品に組み込んでも、ソースを公開する必要はない

> **よくある勘違い** GPL のソース公開義務は「**配布したとき**」に生じます。社内で使うだけなら義務はありません。また「OSS には著作権がない」も誤りで、著作権はあり、その上で利用条件を緩めているだけです。

## パッケージ管理

npm、pip、Maven などの **パッケージ管理システム** は、ライブラリの入手・バージョン解決・依存関係の管理を自動化します。便利な反面、次の注意が必要です。

- **依存の依存**（推移的依存）まで含めると膨大になり、脆弱性の混入経路になる
- バージョンを固定しないと、環境ごとに異なるものが入り再現性が失われる
- 名前の似た偽パッケージを入れさせる **タイポスクワッティング** や、供給元を汚染する **サプライチェーン攻撃** の標的になる

対策は、**バージョンの固定**、**ソフトウェア部品表（SBOM）による構成把握**、**脆弱性情報の継続的な確認** です。

# この節のまとめ

- IDE は道具一式をまとめたもの。**デバッガは動かして調べる、静的解析は動かさずに調べる**
- バージョン管理は変更履歴を全部残すしくみ。**同じ行を 2 人が変えた競合は人間が解決する**
- コンテナは OS を共有するので軽く、「自分の環境では動いた」問題を防げる
- OSS のライセンスは 2 系統。**GPL は組み込んだ製品も公開する義務、MIT や BSD は表示だけでよい**
- GPL の公開義務は**配布するときに生じる**。社内利用だけなら義務はない

# 関連する節と練習

- [開発プロセスとアジャイル](textbook/t-dev-2)　アジャイルと CI/CD
- [法務と標準化](textbook/s-law-2)　OSS ライセンスの法的な扱い
`,
  },
  {
    id: 't-sw-4',
    categoryId: 't-sw',
    title: 'ジョブ管理とファイル操作の実務',
    goal: 'ジョブ管理とスプーリングの仕組みを説明し、シェル操作とアクセス権を読み解ける',
    minutes: 20,
    body: `
# ざっくり言うと

この節は、**コンピュータを実際に動かす現場の作法**を扱います。

前半のジョブ管理は、**夜間にまとめて処理を流す**ような使い方の話です。給与計算や売上集計のように「決まった時刻に、決まった順序で、大量に」処理する仕事は今も多く、その手順の管理には名前が付いています。途中で失敗したときにどこからやり直すか、という話も含まれます。

後半のコマンド操作は、**画面のアイコンをクリックする代わりに、文字で命令する**やり方です。慣れないうちは不便に見えますが、同じ作業を 1000 回繰り返すときは圧倒的に速い。試験では、文字の並びを読んで「どのファイルが対象になるか」「どこに出力されるか」を答えさせます。**規則は少ないので、覚えれば確実に取れます。**

# ジョブ管理

**ジョブ** は、利用者から見た仕事の 1 単位です。ジョブは複数の **ジョブステップ**（コンパイル、リンク、実行など）に分かれ、前のステップの結果によって次に進むかどうかが決まります。

OS の **ジョブ管理** は、投入されたジョブを受け付け、実行順序を決め、資源を割り当て、終了を通知します。この一連を担うのが **ジョブスケジューラ** です。

\`\`\`diagram:flow
title: ジョブが処理されるまで
ジョブの投入 :: 実行してほしい仕事を登録
入力待ち行列 :: 優先度順に並べて待たせる
ジョブステップ 1 :: 例）コンパイル
ジョブステップ 2 :: 例）実行
出力待ち行列 :: 結果を印刷・出力へ回す
終了通知 :: 完了を利用者に知らせる
note: マスタスケジューラが全体を統括し、ジョブスケジューラが個々のジョブの進行を管理します。
\`\`\`

## スプーリング

低速な入出力装置（プリンタなど）を待って CPU が遊ぶのを防ぐ仕組みです。出力データをいったん **高速な補助記憶（スプールファイル）に書き出し**、プログラムはすぐ次の処理へ進みます。実際の印刷は裏で順次行われます。

\`\`\`diagram:seq
title: スプーリングの流れ
actors: 応用プログラム | スプールファイル | プリンタ
応用プログラム -> スプールファイル :: 出力データを高速に書き出す
スプールファイル -> 応用プログラム :: 書込み完了。プログラムは次の処理へ
スプールファイル -> プリンタ :: 裏で順番に印刷する
note: 目的は CPU の待ち時間削減、つまり単位時間当たりの処理量（スループット）の向上です。
\`\`\`

> **試験のポイント** スプーリングの目的は「印刷を速くすること」ではなく、**低速装置の完了を待たずに CPU を使い続けること** です。ターンアラウンドタイム（投入から結果受取りまで）よりも、スループットの改善に効きます。

**スプーリング**は、印刷を待たずに次の仕事へ進むための工夫です。プリンタは CPU に比べて桁違いに遅いので、印刷が終わるまで待っていたら CPU が遊んでしまいます。そこで、印刷したい内容をいったんディスクに書き出しておき、CPU は次の仕事に移る。あとはプリンタが自分のペースで、そのファイルを読みながら印刷します。

> **よくある勘違い** スプーリングの目的は「印刷を速くすること」ではありません。印刷そのものにかかる時間は変わらず、**CPU を待たせないこと**が目的です。

## バッチ処理のリランとリカバリ

夜間バッチのように大量データを一括処理するジョブでは、途中で異常終了したときの立て直し方をあらかじめ決めておきます。

| 用語 | 内容 |
|---|---|
| **リラン** | 処理前の状態に戻し、最初からやり直す |
| **リスタート（リカバリ）** | **チェックポイント** まで戻し、その続きから再開する |
| **リトライ** | 一時的な障害とみなし、同じ処理をもう一度試す |

長時間のバッチほど、最初からやり直す損失が大きいため、**途中経過を残して再開できる設計** が重要になります。また、同じジョブを二重に流しても結果が壊れないよう、**再実行時は処理済みデータを除外する** 作りにします。

# シェルとコマンド操作

**シェル** は利用者の入力したコマンドを解釈して OS に渡すプログラムです。カーネルの外側にあり、利用者との対話を担当します。一連のコマンドをファイルにまとめたものが **シェルスクリプト** で、定型作業の自動化に使います。

## ワイルドカード

ファイル名をまとめて指定する記号です。

| 記号 | 意味 | 例 |
|---|---|---|
| \`*\` | 任意の長さの文字列（0 文字含む） | \`*.txt\` → 拡張子が txt のすべて |
| \`?\` | 任意の 1 文字 | \`a?.csv\` → a1.csv、ab.csv |
| \`[ ]\` | かっこ内のどれか 1 文字 | \`data[123].log\` → data1.log など |

> **試験のポイント** \`?\` は **必ず 1 文字**、\`*\` は **0 文字でもよい**。\`a*.txt\` は a.txt にも一致しますが、\`a?.txt\` は一致しません。

## リダイレクトとパイプ

| 記号 | 意味 |
|---|---|
| \`>\` | 標準出力をファイルへ（**上書き**） |
| \`>>\` | 標準出力をファイルへ（**追記**） |
| \`<\` | ファイルを標準入力として読み込む |
| パイプ（縦棒） | 前のコマンドの出力を次のコマンドの入力にする |

\`\`\`
ls > list.txt     … list.txt を新規作成（既存内容は消える）
ls >> list.txt    … list.txt の末尾に追記
sort < data.txt   … data.txt を標準入力として sort に渡す
ls | sort         … ls の結果をそのまま sort に流し込む
\`\`\`

「\`>\` は上書きなので既存の内容が消える、\`>>\` は末尾に足す」という違いが問われます。パイプは、小さな道具をつないで目的の処理を組み立てるという考え方の中核です。

記号は少ないので、意味を 1 度覚えれば読めます。星印は「**何文字でもよい（0 文字も可）**」、疑問符は「**必ず 1 文字**」。

> **よくある勘違い** 星印と疑問符を同じ扱いにすると必ず間違えます。ファイル名 a.txt は、a のあとに文字がないので「a に続く任意の文字列」を表す a 星印 .txt には一致しますが、「a のあとに必ず 1 文字」を表す a 疑問符 .txt には**一致しません**。

# ファイルのアクセス権

UNIX 系 OS では、ファイルごとに **所有者・グループ・その他** の 3 者に対し、**読取り（r）・書込み（w）・実行（x）** の可否を設定します。1 者あたり 3 ビット、合計 9 ビットで表現し、8 進数 3 桁で書きます。

\`\`\`diagram:bits
title: パーミッション 755 のビット表現
1 1 1 | 1 0 1 | 1 0 1
r w x r w x r w x
note: 左から 所有者(111=7)、グループ(101=5)、その他(101=5)。読 4 ＋ 書 2 ＋ 実行 1 の合計が各桁の数字になります。
\`\`\`

\`\`\`
755 の意味
  所有者   : 4+2+1 = 7 → 読取り・書込み・実行すべて可
  グループ : 4+0+1 = 5 → 読取りと実行のみ（書込み不可）
  その他   : 4+0+1 = 5 → 読取りと実行のみ

644 の意味
  所有者   : 4+2+0 = 6 → 読取り・書込み
  グループ : 4+0+0 = 4 → 読取りのみ
  その他   : 4+0+0 = 4 → 読取りのみ
\`\`\`

> **試験のポイント** 数字とビットの対応（**読 = 4、書 = 2、実行 = 1**）を覚えれば、どの組合せも足し算で出せます。「その他の利用者に書込みを許してはいけない」という **最小権限の原則** の観点で正誤を判断させる出題もあります。

なお、ディレクトリに対する x は「そのディレクトリの中に入れる（配下をたどれる）」権限を意味し、r は「中のファイル名一覧を見られる」権限を意味します。混同しやすい点なので区別しておきましょう。

# この節のまとめ

- スプーリングは、遅い装置の完了を待たずに **CPU を次の仕事へ進めるための工夫**
- 失敗したときは、最初からやり直すのがリラン、**途中の記録地点から再開するのがリスタート**
- コマンドの星印は「0 文字でもよい任意の文字列」、疑問符は「必ず 1 文字」
- 出力リダイレクトは記号 1 つで上書き、2 つ重ねると追記
- アクセス権は **読 4・書 2・実行 1** の足し算で表す

# 関連する節と練習

- [システムの性能評価](textbook/t-sys-2)　ターンアラウンドタイム
- [運用の実務と可用性](textbook/m-sm-2)　運用設計の実務
`,
  },
];
